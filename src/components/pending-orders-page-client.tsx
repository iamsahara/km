"use client";

import { RoutePage } from "@/components/route-page";

export function PendingOrdersPageClient() {
  return (
    <RoutePage
      title="Pending Orders"
      description="Follow incoming supply orders until they are received."
    />
  );
}
